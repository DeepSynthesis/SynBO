from setuptools import setup, find_packages

setup(
    name="rxnopt",
    version="0.0.1",
    packages=find_packages(),
    install_requires=[
        # 'numpy>=1.20.0',
        # 'scipy>=1.7.0',
        # 其他依赖
    ],
)
